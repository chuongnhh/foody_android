<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chuongnh.hcm.foody">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.Hello.HelloActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Main.MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".View.Home.HomeActivity" />
        <activity android:name=".View.Collection.CollectionActivity" />
        <activity android:name=".View.Account.AccountActivity" />
        <activity android:name=".View.Search.SearchActivity" />
        <activity android:name=".View.Notification.NotificationActivity" />
        <activity android:name=".View.Category.CategoryActivity"></activity>
    </application>

</manifest>